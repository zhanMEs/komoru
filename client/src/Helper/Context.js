import { createContext } from "react";

export const Exam1Context = createContext({});
export const Exam2Context = createContext({});
export const Exam3Context = createContext({});
export const Exam4Context = createContext({});
export const Exam5Context = createContext({});

export const BookContext = createContext({});
